<?php
namespace App\Models;

/**
 * Settings Model Wrapper (Plural Alias)
 * Inherits all capabilities from Setting for consistency with the settings table.
 */
class Settings extends Setting {
    // All static methods (getAll, getVal, setVal) are inherited from Setting
}
